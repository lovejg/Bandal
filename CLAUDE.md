# Bandal

배달 최소주문금액을 여러 명이 나눠 채우는 공동구매 서비스.
설계는 `DESIGN.md`, 결정 이력은 `docs/DECISIONS.md`, 트러블슈팅은 `docs/JOURNAL.md`.

## 작업 방식

포트폴리오 메인 프로젝트이자 학습 프로젝트다. 코드가 빨리 나오는 것보다 사용자가
이해한 채로 넘어가는 게 우선이다.

### 역할 분담

- **Claude가 깐다**: 프로젝트 초기 설정, 인프라(Docker Compose, CI 설정), 보일러플레이트.
  삽질해도 남는 게 적은 부분.
- **새 기능의 디렉토리와 뼈대 파일은 Claude가 만든다.** 클래스, 메서드 시그니처, TODO 주석까지
  깔아두고 컴파일되는 상태로 넘긴다. 사용자는 TODO를 채운다.
- **사용자가 짠다**: 도메인 로직(정산 계산기, 상태 전이), 동시성 해결. 이 프로젝트의
  알맹이. 막히면 같이 본다.
- **테스트는 Claude가 알아서 쓴다.** 단계마다 사용자 리뷰를 기다리지 않는다.
  사용자는 나중에 따로 코드 리뷰를 하고 궁금한 걸 묻는다. 안내할 때는 테스트 설명보다
  "무엇을 구현해야 하는지"에 집중한다.
- Claude가 코드를 쓸 때는 **왜 그렇게 했는지 설명을 곁들인다.** 설명 없이 코드만
  던지지 않는다. 사용자가 아직 Spring/JPA에 익숙하지 않다는 걸 전제로 한다.

### 한 번에 한 단계

구현은 한 단계씩 진행한다. 한 번에 여러 단계를 몰아서 안내하지 않는다.
한 단계 = 테스트 작성(Claude) -> 구현(사용자) -> 통과 확인(Claude가 돌려서 확인).
그 단계가 끝나면 다음 단계를 준다.

### 설계는 먼저 대화로

새 기술이나 구조가 들어올 때 — Spring Batch를 어디에 쓸지, 어디서 동시성 문제가 날지,
어디에 트래픽이 몰릴지 — 바로 구현하지 않고 먼저 이야기한다. 대안을 놓고 비교하고,
결론과 버린 대안을 `docs/DECISIONS.md`에 남긴다.

### 먼저 깨뜨리고 고친다

순진한 버전을 먼저 만들어서 **실제로 실패하는 걸 본 다음** 고친다. 특히 Phase 3
동시성은 반드시 이 순서로. 에러 메시지를 직접 읽는 게 학습의 핵심이다.

예상한 에러는 기록하지 않는다. 재현한 것만 기록한다. 피해 간 문제는 "피해 갔다"고 적는다.

### 기록

겪는 즉시 남긴다. 나중에 몰아서 쓰면 기억이 안 난다.

- `docs/DECISIONS.md` — 왜 이렇게 정했는지. 버린 대안을 반드시 같이 적는다.
  면접에서 "왜 이걸 선택했나요"에 답할 재료.
- `docs/JOURNAL.md` — 실제로 겪은 문제와 해결 과정. "가장 어려웠던 문제"에 답할 재료.

## 코드 스타일

- 주석은 평범한 한 줄 주석. Javadoc HTML 태그(`<p>`, `<b>`, `{@code}`, `{@link}`)는
  쓰지 않는다. 에디터에서 읽히는 글이지 문서 생성물이 아니다.
- 짧은 문장, 평범한 한국어.

## 환경

프로젝트가 WSL 경로에 있는데 Windows PowerShell에는 git이 없다. git은 WSL 쪽에서 실행.
